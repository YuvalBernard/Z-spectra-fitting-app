[project]
name = "z-spectra-fitting-app"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    arviz,
    jax,
    lmfit,
    matplotlib,
    numpy,
    numpyro,
    openpyxl>,
    pandas,
    pyqt6,
    pyqt6-sip,
    pyqtgraph,
    pyqtwaitingspinner,
    toml>,
]
